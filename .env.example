USE_LLM=1
LLM_PROVIDER=gemini
GEMINI_API_KEY=
GEMINI_MODEL=gemini-2.5-flash
GEMINI_MAX_OUTPUT_TOKENS=1024

USE_DUMMY_ENGINE=0

# USI Engine binary (example: /usr/local/bin/yaneuraou)
ENGINE_CMD=/usr/local/bin/yaneuraou

# Absolute paths to engine resources (replace USER with your username)
ENGINE_EVAL_DIR=/home/USER/engines/yaneuraou/eval
ENGINE_BOOK_DIR=/home/USER/engines/yaneuraou/book

# Default USI options (set USI_OwnBook=false if you don't have a book)
ENGINE_USI_OPTIONS=Threads=8,USI_Hash=256,USI_OwnBook=false

# NOTE: copy this file to .env and edit values before running scripts
# Replace '/home/USER' with your actual user home directory path
# --- LLM ---
USE_LLM=1
LLM_PROVIDER=gemini
GEMINI_API_KEY=REPLACE_ME
GEMINI_MODEL=gemini-2.5-flash
GEMINI_MAX_OUTPUT_TOKENS=1024

# --- USI Engine ---
# どちらか利用: /usr/local/bin/yaneuraou or ~/engines/yaneuraou/bin/yaneuraou
ENGINE_CMD=/usr/local/bin/yaneuraou

# 実ファイルの置き場（絶対パス）
ENGINE_EVAL_DIR=/home/USER/engines/yaneuraou/eval
ENGINE_BOOK_DIR=/home/USER/engines/yaneuraou/book

# book.bin がない場合は USI_OwnBook=false に変えてください（user名を実ユーザー名に置換）
ENGINE_USI_OPTIONS=Threads=8,USI_Hash=256,USI_OwnBook=true,BookFile=book.bin,BookDir=/home/USER/engines/yaneuraou/book,FlippedBook=true,BookDepthLimit=32

# Example: change /home/USER to /home/yourusername
# NOTE: copy this file to .env and edit values before running scripts
