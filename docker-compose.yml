services:
  engine:
    build:
      context: ./engine
    ports:
      - "8081:8081"

  backend:
    build:
      context: ./backend
    environment:
      ENGINE_URL: "http://engine:8081"   # ← ここをこれだけにする（/analyze を消す。末尾スラッシュも不要）
    ports:
      - "8000:8000"
    depends_on:
      - engine
